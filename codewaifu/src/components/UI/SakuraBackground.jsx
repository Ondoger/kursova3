/* Sakura background neutralised for the GitHub-native theme.
 * Component intentionally renders nothing so existing usages
 * (`<SakuraBackground count={...} />`) keep working.
 */
export function SakuraBackground() {
  return null;
}
